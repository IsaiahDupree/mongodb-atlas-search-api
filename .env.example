# MongoDB Atlas connection string
# For local development, you can use the local MongoDB instance
MONGODB_URI=mongodb://mongodb:27017/productdb

# When using MongoDB Atlas, replace with your connection string
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/productdb?retryWrites=true&w=majority

# API key for authorization (used in x-apikey header)
API_KEY=your_default_api_key
